{
  system.defaults.CustomSystemPreferences = {
    "/Library/Preferences/com.apple.iokit.AmbientLightSensor.plist" = {
      "Automatic Display Enabled" = false; # disable auto-dimming internal display, it causes it to dim when waking from sleep while not on battery which is annoying
    };
  };
}
